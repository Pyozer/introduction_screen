name: introduction_screen
description: Introduction/Onboarding package for flutter app with some customizations possibilities
version: 3.2.0
homepage: https://github.com/AnimaSelf/introduction_screen

publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  collection: ^1.18.0
  dots_indicator: ^3.0.0
  flutter_keyboard_visibility: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
